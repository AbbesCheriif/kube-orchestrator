[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --cov=kube_orchestrator
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=100
    -v
markers =
    unit: unit tests (deselect with '-m "not unit"')
    integration: integration tests requiring a real cluster
    slow: slow tests (deselect with '-m "not slow"')
