apiVersion: v1
kind: ConfigMap
metadata:
  name: full-app-config
  namespace: default
  labels:
    app: full-app
data:
  APP_ENV: production
  LOG_LEVEL: INFO
  DB_HOST: postgres.default.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: myapp
