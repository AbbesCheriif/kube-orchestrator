apiVersion: v1
kind: Service
metadata:
  name: full-app
  namespace: default
  labels:
    app: full-app
spec:
  selector:
    app: full-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
